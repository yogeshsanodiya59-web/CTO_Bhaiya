spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require}
    username: ${SPRING_DATASOURCE_USERNAME:postgres.jocfhsetgbwgaruojagr}
    password: ${SPRING_DATASOURCE_PASSWORD:iqre7tiur4@59}
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update # Automatically update schema (e.g. add password column)
    show-sql: true
    properties:
      hibernate:
        format_sql: true


server:
  port: ${PORT:8080}

logging:
  level:
    org.springframework.security: DEBUG
